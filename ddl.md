# SQL data definition language (DDL)

```sqlite
create table BASE_USER
(
	USER_ID CHAR(16) FOR BIT DATA not null
		primary key,
	CREATED TIMESTAMP not null,
	EMAIL VARCHAR(255) not null
		unique,
	NICKNAME VARCHAR(255)
		constraint UK_32FYBP3BSDBUPNF499NFEOJ1C
			unique,
	OAUTH VARCHAR(4696) not null
		unique
);
create index IDXTMWXJGD8U97V7076BH5PJ0L91
	on BASE_USER (CREATED);
create table STUDENT
(
	STUDENT_ID CHAR(16) FOR BIT DATA not null
		primary key,
	STUDENT_NAME VARCHAR(255),
	USER_ID CHAR(16) FOR BIT DATA
		constraint FK1JEUKVBPTTK4J1V8IMCJC3NPO
			references BASE_USER
);
create table TEACHER
(
	TEACHER_ID CHAR(16) FOR BIT DATA not null
		primary key,
	TEACHER_NAME VARCHAR(255)
		constraint UK_MDUDQ3FKCQOW8IRT5GCC6CBXJ
			unique,
	USER_ID CHAR(16) FOR BIT DATA
		constraint FKECDL73AYA6QA435NABWRB6STI
			references BASE_USER
);
create table APPOINTMENT
(
	APPOINTMENT_ID CHAR(16) FOR BIT DATA not null
		primary key,
	APPOINTMENT_DATE DATE not null,
	CREATED TIMESTAMP not null,
	END_TIME TIMESTAMP not null,
	START_TIME TIMESTAMP not null,
	STATUS INTEGER not null,
	SUBJECT INTEGER,
	UPDATED TIMESTAMP not null,
	STUDENT_ID CHAR(16) FOR BIT DATA
		constraint FKQMGDC0SMRFP9823RJUGQK97NM
			references STUDENT,
	TEACHER_ID CHAR(16) FOR BIT DATA
		constraint FKMLBF2026PFJLRFNYQ72TG3035
			references TEACHER
);
create index IDX7DH64EEXOAWN8RIO1D54X7V4R
	on APPOINTMENT (CREATED);
create index IDXKO8QD6N0WT9WGQ61VD2W8R3WB
	on APPOINTMENT (START_TIME);
create index IDXKQAVGWJMXR5E34LBEG5RTTE9E
	on APPOINTMENT (STATUS);
create index IDXLLISG0HTXS55IY7CS99J3DCT5
	on APPOINTMENT (END_TIME);
create table POLICY
(
	POLICY_ID CHAR(16) FOR BIT DATA not null
		primary key,
	BLOCK_TIME INTEGER not null,
	CREATED TIMESTAMP not null,
	DATE TIMESTAMP not null,
	STATUS INTEGER not null,
	SUBJECT INTEGER,
	UPDATED TIMESTAMP not null,
	TEACHER_ID CHAR(16) FOR BIT DATA
		constraint FKGGY7DCP7O9CA74LLHN9J94PFY
			references TEACHER
);
create index IDX6L90HM2KQU4M9OS762CG1YH5D
	on POLICY (CREATED);
create schema SA;
create table BASE_USER
(
	USER_ID CHAR(16) FOR BIT DATA not null
		primary key,
	CREATED TIMESTAMP not null,
	EMAIL VARCHAR(255) not null
		unique,
	NICKNAME VARCHAR(255)
		constraint UK_32FYBP3BSDBUPNF499NFEOJ1C
			unique,
	OAUTH VARCHAR(4696) not null
		unique
);
create index IDXTMWXJGD8U97V7076BH5PJ0L91
	on BASE_USER (CREATED);
create table STUDENT
(
	STUDENT_ID CHAR(16) FOR BIT DATA not null
		primary key,
	STUDENT_NAME VARCHAR(255),
	USER_ID CHAR(16) FOR BIT DATA
		constraint FK1JEUKVBPTTK4J1V8IMCJC3NPO
			references BASE_USER
);
create table TEACHER
(
	TEACHER_ID CHAR(16) FOR BIT DATA not null
		primary key,
	TEACHER_NAME VARCHAR(255)
		constraint UK_MDUDQ3FKCQOW8IRT5GCC6CBXJ
			unique,
	USER_ID CHAR(16) FOR BIT DATA
		constraint FKECDL73AYA6QA435NABWRB6STI
			references BASE_USER
);
create table APPOINTMENT
(
	APPOINTMENT_ID CHAR(16) FOR BIT DATA not null
		primary key,
	APPOINTMENT_DATE DATE not null,
	CREATED TIMESTAMP not null,
	END_TIME TIMESTAMP not null,
	START_TIME TIMESTAMP not null,
	STATUS INTEGER not null,
	SUBJECT INTEGER,
	UPDATED TIMESTAMP not null,
	STUDENT_ID CHAR(16) FOR BIT DATA
		constraint FKQMGDC0SMRFP9823RJUGQK97NM
			references STUDENT,
	TEACHER_ID CHAR(16) FOR BIT DATA
		constraint FKMLBF2026PFJLRFNYQ72TG3035
			references TEACHER
);
create index IDX7DH64EEXOAWN8RIO1D54X7V4R
	on APPOINTMENT (CREATED);
create index IDXKO8QD6N0WT9WGQ61VD2W8R3WB
	on APPOINTMENT (START_TIME);
create index IDXKQAVGWJMXR5E34LBEG5RTTE9E
	on APPOINTMENT (STATUS);
create index IDXLLISG0HTXS55IY7CS99J3DCT5
	on APPOINTMENT (END_TIME);
create table POLICY
(
	POLICY_ID CHAR(16) FOR BIT DATA not null
		primary key,
	BLOCK_TIME INTEGER not null,
	CREATED TIMESTAMP not null,
	DATE TIMESTAMP not null,
	STATUS INTEGER not null,
	SUBJECT INTEGER,
	UPDATED TIMESTAMP not null,
	TEACHER_ID CHAR(16) FOR BIT DATA
		constraint FKGGY7DCP7O9CA74LLHN9J94PFY
			references TEACHER
);
create index IDX6L90HM2KQU4M9OS762CG1YH5D
	on POLICY (CREATED);
```

[Download](ddl.sql)
